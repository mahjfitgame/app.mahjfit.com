/** Value and selection modes supported by the standalone datetime control. */
export enum FormFieldDatetimeModeEnum {
    DATE = 'date',
    TIME = 'time',
    DATETIME = 'datetime',
    DATETIME_RANGE = 'datetime_range',
}

/** Supported overlay modes for the input-associated Owl date-time picker. */
export enum FormFieldDatetimePickerModeEnum {
    POPUP = 'popup',
    DIALOG = 'dialog',
}

/** Initial calendar view shown when the picker opens. */
export enum FormFieldDatetimeStartViewEnum {
    MONTH = 'month',
    YEAR = 'year',
    MULTI_YEARS = 'multi-years',
}
