/** Host-neutral keyboard styles supported by the platform façade. */
export enum PlatformKeyboardStyleEnum {
  Dark = 'DARK',
  Light = 'LIGHT',
  Default = 'DEFAULT',
}

/** Host-neutral keyboard resize modes supported by the platform façade. */
export enum PlatformKeyboardResizeEnum {
  Body = 'body',
  Ionic = 'ionic',
  Native = 'native',
  None = 'none',
}
